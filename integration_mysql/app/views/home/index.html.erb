<div style="max-width: 800px; margin: 40px auto; padding: 20px; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;">
  <h1 style="color: #333; margin-bottom: 20px;">BetterStructureSql Integration App</h1>

  <div style="background: #f5f5f5; padding: 20px; border-radius: 8px; margin-bottom: 20px;">
    <h2 style="color: #666; font-size: 18px; margin-bottom: 10px;">Schema Versions Status</h2>
    <p style="font-size: 16px; margin: 5px 0;">
      <strong>Total Schema Versions:</strong> <%= @schema_versions_count %>
    </p>
    <% if @latest_version %>
      <p style="font-size: 16px; margin: 5px 0;">
        <strong>Latest Version:</strong> <%= @latest_version.created_at.strftime('%Y-%m-%d %H:%M:%S') %>
      </p>
      <p style="font-size: 16px; margin: 5px 0;">
        <strong>PostgreSQL Version:</strong> <%= @latest_version.pg_version %>
      </p>
      <p style="font-size: 16px; margin: 5px 0;">
        <strong>Format:</strong> <%= @latest_version.format_type %>
      </p>
    <% end %>
  </div>

  <div style="background: #e8f4f8; padding: 20px; border-radius: 8px; border-left: 4px solid #0066cc;">
    <h2 style="color: #0066cc; font-size: 18px; margin-bottom: 10px;">Environment Information</h2>
    <p style="font-size: 14px; margin: 5px 0;">
      <strong>Rails Version:</strong> <%= Rails.version %>
    </p>
    <p style="font-size: 14px; margin: 5px 0;">
      <strong>Ruby Version:</strong> <%= RUBY_VERSION %>
    </p>
    <p style="font-size: 14px; margin: 5px 0;">
      <strong>Environment:</strong> <%= Rails.env %>
    </p>
    <p style="font-size: 14px; margin: 5px 0;">
      <strong>Database:</strong> <%= ActiveRecord::Base.connection.adapter_name %>
    </p>
  </div>

  <div style="margin-top: 30px; padding: 20px; background: #fff; border: 1px solid #ddd; border-radius: 8px;">
    <h2 style="color: #333; font-size: 18px; margin-bottom: 15px;">Database Schema</h2>
    <p style="font-size: 14px; margin: 10px 0; color: #666;">
      The database has been initialized with sample users and posts. The schema includes PostgreSQL extensions (pgcrypto, uuid-ossp), custom indexes, and foreign keys.
    </p>
  </div>

  <div style="margin-top: 20px; padding: 15px; background: #f9f9f9; border-radius: 8px; font-size: 14px; color: #666;">
    <p style="margin: 0;">
      This is a Docker-based development environment for testing the BetterStructureSql gem.
      The gem is mounted as a local dependency, so changes to the gem source will reflect without rebuilding.
    </p>
  </div>
</div>
